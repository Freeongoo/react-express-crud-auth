1. Нужно ведь дожидаться загрузки данных с API - и только после загрузки отображать?
2. Почему нужно использовать ссылки через Link?
3. в /src/app/components/user/FormData.js
    3.1 если я ожидаю object - user - нужно ли мне в конструкторе инициализировать этот объект?
        this.state = {
            user: {
                firstName: '',
                lastName: '',
                email: ''
            }
        }
    3.2 как правильно обработать изменение props - как в этом компоненте - меняется id - из урла и передается
    в props - но в первоначальной отрисовке - передается путое значение id - а потом уже когда родитель
    отрисовался - он уже передаст корректный id
        - я обработал с помощью проверки
            componentDidUpdate(prevProps, prevState) {
                if (prevProps.userId !== this.props.userId) {
                    Api.getUserById(this.props.userId, (data) => {this.setState({user: data})})
                }
            }
            возможно нужно было подругому это обрабатывать - чтобы не определять напрямую

    3.3 как правильнее?
        этот метод типа устарел...
        componentWillReceiveProps(nextProps) {
            if (nextProps.userId !== this.props.userId) {
                Api.getUserById(nextProps.userId, (data) => {this.setState({user: data})})
            }
        }

        componentDidUpdate(prevProps, prevState) {
            if (prevProps.userId !== this.props.userId) {
                Api.getUserById(this.props.userId, (data) => {this.setState({user: data})})
            }
        }
4. Если еще нету данных как в edit.js - где ожидается объект user - то лучше не инициализировать
первоначальными значениями? а крутить спинер?